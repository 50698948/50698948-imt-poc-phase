# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=imt_poc
DB_USER=imt
DB_PASSWORD=imt_poc_2024

# Local embedding (PoC uses random projection — no model download needed)
LOCAL_EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIM=384
